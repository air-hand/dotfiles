name: "Update PRs auto merge enabled"

on:
  push:
    branches:
      - main

permissions: {}

jobs:
  update-auto-merge-enabled:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: gh-app-token
        with:
          client-id: ${{ vars.GH_APP_CLIENT_ID }}
          private-key: ${{ secrets.GH_APP_PEM_FILE }}
          permission-contents: write
          permission-pull-requests: write
          permission-workflows: write
      - name: update
        uses: air-hand/common-actions/update-prs-auto-merge-enabled@c226b3a14b3cfface85ca278f19ee506cb845574 # v1.3.1
        with:
          token: ${{ steps.gh-app-token.outputs.token }}
          base_branch: main
          # Using 'merge' as the update method to avoid commit signature issues.
          update_method: merge
