{{ $interactive := stdinIsATTY }}

{{/* skip apply .gitconfig on devcontainer */}}
{{ $git_name := "" }}
{{ $git_email := "" }}
{{- if $interactive }}
{{ $git_name = promptString "What is your name for git." }}
{{ $git_email = promptString "What is your email for git." }}
{{- end }}

[edit]
    command = "vim"
    apply = true

[data]
    git_name = {{ $git_name | quote }}
    git_email = {{ $git_email | quote }}

